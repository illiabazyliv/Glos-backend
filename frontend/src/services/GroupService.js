const GroupService = {
    getUserGroups() { // GET /users/{username}/groups

    },

    getGroup() { // GET /users/{username}/groups/{groupName}

    },

    createGroup() { // PUT /users/{username}/group/{groupName}

    },

    updateGroup() { // PATCH /users/{username}/groups/{groupName}

    },

    deleteGroup() { // DELETE /users/{username}/groups/{groupName}

    },
};

export default GroupService;