import { NavLink, Outlet } from 'react-router-dom';

function FilesPage() {
    return (
        <div className="inner-page">
            page content here
        </div>

    );
}

export default FilesPage;